# Solution to my class problems

I love inline code and functional programming.

```py
print([state for state in hateMyself])
```

```js
console.log(
  Object.entries(loveMyself).map(([key]) => ({
    [key]: Object.keys(hateMyself),
  }))
)
```
